export interface User {
    _id:string,
    name:string,
    email:string,
    password:string,
    date:string
}
export interface IUser {
    body: string;
    id: number;
    title: string;
    userId: number;
 }
